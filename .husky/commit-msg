#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

COMMIT_MSG_FILE="${1:-.git/COMMIT_EDITMSG}"
if [ -f "$COMMIT_MSG_FILE" ]; then
  cat "$COMMIT_MSG_FILE" | npx --no commitlint
else
  echo "Warning: Commit message file not found: $COMMIT_MSG_FILE"
  exit 0
fi





